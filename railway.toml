# Railway.toml configuration for VTP Platform
# This file configures Railway deployment settings

[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "./main"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
